<?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">
    <!-- Full replacement: Custom right side panel without Sales and Profitability sections -->
    <t t-name="project.ProjectRightSidePanel" owl="1">
        <div t-if="projectId" class="o_rightpanel pt-0 bg-view border-start overflow-auto">
            <ProjectRightSidePanelSection
                name="'stat_buttons'"
                header="false"
                show="!!state.data.buttons"
            >
                <div class="o_form_view">
                    <div class="oe_button_box o-form-buttonbox d-flex flex-wrap">
                        <t t-foreach="state.data.buttons" t-as="button" t-key="button.action">
                            <ViewButton
                                t-if="button.show and button.action !== 'action_view_sos'"
                                defaultRank="'oe_stat_button'"
                                className="'h-auto py-2 border border-start-0 border-top-0 text-start'"
                                icon="`fa-${button.icon}`"
                                title="button.text"
                                clickParams="_getStatButtonClickParams(button)"
                                record="_getStatButtonRecordParams()"
                            >
                                <t t-set-slot="contents">
                                    <div class="o_field_widget o_stat_info">
                                        <span class="o_stat_value text-start">
                                            <t t-esc="button.number"/>
                                        </span>
                                        <span class="o_stat_text">
                                            <t t-esc="button.text"/>
                                        </span>
                                    </div>
                                </t>
                            </ViewButton>
                        </t>
                    </div>
                </div>
            </ProjectRightSidePanelSection>
            <!-- Participants section -->
            <ProjectRightSidePanelSection
                name="'participants'"
                show="state &amp;&amp; state.data &amp;&amp; state.data.participants &amp;&amp; state.data.participants.total &gt; 0"
            >
                <t t-set-slot="title" owl="1">
                    Participants
                </t>
                <t t-if="state &amp;&amp; state.data &amp;&amp; state.data.participants">
                    <button
                        t-if="state.data.participants.pending &gt; 0"
                        onclick="markAllParticipantsCompleted(event)"
                        t-att-data-project-id="projectId"
                        class="btn btn-success mb-2 w-100"
                    >
                        Mark All Test Completed
                    </button>
                    <table class="table table-sm">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Mobile</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr t-foreach="state.data.participants.data" t-as="p" t-key="p.id">
                                <td><t t-esc="p.first_name"/> <t t-esc="p.last_name"/></td>
                                <td><t t-esc="p.email_address"/></td>
                                <td><t t-esc="p.mobile_phone"/></td>
                                <td><t t-esc="p.state"/></td>
                                <td>
                                    <button t-if="p.state === 'not_yet_confirmed'" onclick="setParticipantConfirmed(event)" t-att-data-id="p.id" class="btn btn-success btn-sm" title="Complete">
                                        <i class="fa fa-check"/>
                                    </button>
                                    <button t-if="p.state === 'not_yet_confirmed'" onclick="setParticipantRescheduled(event)" t-att-data-id="p.id" class="btn btn-warning btn-sm" title="Reschedule">
                                        <i class="fa fa-clock-o"/>
                                    </button>
                                    <button t-if="p.state === 'not_yet_confirmed'" onclick="setParticipantCancelled(event)" t-att-data-id="p.id" class="btn btn-danger btn-sm" title="Cancel">
                                        <i class="fa fa-times"/>
                                    </button>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </t>
            </ProjectRightSidePanelSection>
            <ProjectRightSidePanelSection
                name="'milestones'"
                show="!!state.data.milestones &amp;&amp; !!state.data.milestones.data"
            >
                <t t-set-slot="header" owl="1">
                    <span class="btn btn-secondary">
                        <div class="o_add_milestone">
                            <a t-on-click="addMilestone">Add Milestone</a>
                        </div>
                    </span>
                </t>
                <t t-set-slot="title" owl="1">
                    Milestones
                </t>
                <div t-foreach="state.data.milestones.data" t-as="milestone" t-key="milestone.id" class="o_rightpanel_data_row">
                    <ProjectMilestone context="context" milestone="milestone" open.bind="openFormViewDialog" load.bind="loadMilestones"/>
                </div>
                <span t-if="state.data.milestones.data.length === 0" class="text-muted fst-italic">
                    Track major progress points that must be reached to achieve success.
                </span>
            </ProjectRightSidePanelSection>
        </div>
        <!-- If this is called from notif, multiples updates but no specific project -->
        <div t-else=""/>
    </t>
</templates>