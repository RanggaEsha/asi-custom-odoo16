<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Mark Activity as Done Wizard -->
    <record id="view_activity_mark_done_wizard_form" model="ir.ui.view">
        <field name="name">activity.mark.done.wizard.form</field>
        <field name="model">activity.mark.done.wizard</field>
        <field name="arch" type="xml">
            <form string="Mark Activity as Done">
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="summary" readonly="1"/>
                        </h1>
                        <h2>
                            <field name="lead_name" readonly="1"/>
                        </h2>
                    </div>
                    
                    <group>
                        <group string="Activity Information">
                            <field name="activity_type_id" readonly="1"/>
                            <field name="user_id" readonly="1" widget="many2one_avatar_user"/>
                            <field name="date_deadline" readonly="1"/>
                        </group>
                    </group>
                    
                    <group string="Completion Details">
                        <field name="feedback" widget="html" placeholder="Provide feedback for this completed activity..."/>
                        <field name="attachment_ids" widget="many2many_binary"/>
                    </group>
                    
                    <!-- Hidden fields -->
                    <field name="activity_dashboard_id" invisible="1"/>
                    <field name="activity_id" invisible="1"/>
                </sheet>
                
                <footer>
                    <button name="action_mark_done" string="Mark as Done" type="object" class="btn-primary"/>
                    <button name="action_cancel" string="Cancel" type="object" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
